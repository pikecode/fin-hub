# 钉钉审批全历史同步调整

日期: 2026-09-08

## 目标

审批单同步只保留当前启用的审批模板，不再要求用户手动限定天数范围。

## 调整点

1. 仅同步当前启用的审批模板。
2. 手动“同步审批单”不再限制时间跨度。
3. 默认同步起点改为“尽可能早”的历史起点，由系统自动回补全部可拉取审批。
4. 保留按修改时间重刷入口，用于修复后续变更。

## 结果

- 用户不需要再选择“最近多少天”。
- 历史审批会按当前启用模板持续补齐。
- 仍然依赖模板启用状态控制同步范围。

